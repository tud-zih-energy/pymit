from distutils.core import setup

setup (name = 'pymit',
       version = '0.2',
       description = 'Python Mutual Information Toolbox',
       py_modules = ["pymit"],
       install_requires=['numpy'])
